@extends('bar.layout')

@section('content')
<div class="container">

    <h1>Bar Dashboard</h1>

    <div class="row">

        {{-- Categories --}}
        <!-- <div class="col-md-6 mb-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
                    <h4 class="card-title mb-3">Manage Categories</h4>
                    <p class="text-muted">Create, edit and organize drink categories.</p>
                    <a href="{{ route('bar.categories.index') }}" class="btn btn-primary">
                        Go to Categories
                    </a>
                </div>
            </div>
        </div>

        {{-- Products --}}
        <div class="col-md-6 mb-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
                    <h4 class="card-title mb-3">Manage Products</h4>
                    <p class="text-muted">Add, update and manage bar products.</p>
                    <a href="{{ route('bar.products.index') }}" class="btn btn-primary">
                        Go to Products
                    </a>
                </div>
            </div>
        </div>

    </div> -->

</div>
@endsection
